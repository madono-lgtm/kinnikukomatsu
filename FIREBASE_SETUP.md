# Firebase セットアップ手順

このサイトは Supabase から **Firebase**(Google)に移行しました。
Firebase の無料プラン(Spark)は **放置しても自動停止しない** ため、
以前のように「1週間使わないとサイトが開けなくなる」ことはなくなります。

以下を上から順にやれば動きます。所要 15〜20 分ほどです。

---

## 1. Firebase プロジェクトを作る

1. https://console.firebase.google.com を開き、Google アカウントでログイン
2. 「プロジェクトを作成」→ プロジェクト名は `kinnikukomatsu` など好きな名前でOK
3. Google アナリティクスは「無効」でOK → 作成

## 2. ログイン機能(Authentication)を有効にする

1. 左メニュー「構築」→「Authentication」→「始める」
2. 「ログイン方法」タブ →「メール / パスワード」を選び **有効にする** → 保存

## 3. データベース(Firestore)を作る

1. 左メニュー「構築」→「Firestore Database」→「データベースを作成」
2. ロケーションは **asia-northeast1(東京)** か **asia-northeast2(大阪)** を選択
3. 「本番環境モードで開始」を選んで作成
4. 作成できたら「ルール」タブを開き、中身を全部消して、
   このリポジトリの **`firestore.rules`** ファイルの中身を貼り付け →「公開」

## 4. アプリの設定値を取得して index.html に貼る

1. 左上の歯車 →「プロジェクトの設定」→ 下の「マイアプリ」→ ウェブアイコン `</>` をクリック
2. アプリ名は `web` などでOK →「アプリを登録」(Hosting のチェックは不要)
3. 表示される `firebaseConfig = { ... }` の中身を控える
4. `index.html` の上部にある `FIREBASE_CONFIG` の「ここに〜を貼り付け」の部分を、
   控えた値でそれぞれ置き換える(apiKey / authDomain / projectId /
   storageBucket / messagingSenderId / appId の6つ)

> apiKey はサーバーの秘密鍵ではなく「プロジェクトの住所」なので、
> HTML に書いて公開しても問題ありません。安全はステップ3のルールが守ります。

## 5. 承認済みドメインを追加する

1. 「Authentication」→「設定」タブ →「承認済みドメイン」
2. 「ドメインを追加」→ `madono-lgtm.github.io` を追加

## 6. 公開する

`index.html` の変更(ステップ4)をコミットして main ブランチにプッシュすれば、
GitHub Pages が自動で反映します。数分後にサイトを開き、
**最初にアカウント登録した人が自動でトレーナー(管理者)になります。**

---

## 7.(任意)会員写真機能を使う場合 — Storage

靴・顔・体型ビフォーアフターの写真機能を使う場合のみ必要です。
**2024年10月以降、Firebase Storage は Blaze プラン(従量課金・クレジットカード登録)が
必要**になりました。無料枠(5GBまで)の範囲なら実際の請求はほぼ0円ですが、
カード登録に抵抗があればこのステップは飛ばしてください。
写真機能以外(予約・チケット・ヒアリングシート)は Storage なしで全部動きます。

1. コンソール下部「アップグレード」から Blaze プランに変更
2. 左メニュー「構築」→「Storage」→「始める」→ ロケーションは Firestore と同じものを選択
3. 「ルール」タブに **`storage.rules`** ファイルの中身を貼り付け →「公開」

---

## 旧 Supabase のデータについて

会員・予約・チケット残数のデータは旧 Supabase プロジェクトに残っています。
**停止から90日を過ぎると完全に削除される**ため、残したいデータがある場合は
先に https://supabase.com/dashboard でプロジェクトを Restore(復元)してください。

- 会員数が少ない場合:復元した画面を見ながら、新サイトで再登録+トレーナーが
  チケット枚数を「チケット管理」画面で入力し直すのが一番確実で早いです
- 会員が多くて手作業が大変な場合:データ移行スクリプトを用意できるので相談してください

※ パスワードはどの方法でも移行できません(Supabase から取り出せない仕組みのため)。
   全会員に新サイトでの再登録をご案内ください。

## 料金と停止ポリシー

| | Supabase 無料 | Firebase 無料(Spark) |
|---|---|---|
| 放置時の自動停止 | 1週間で停止 | **停止なし** |
| データベース | 500MB | 1GB・読取5万回/日 |
| 認証 | 5万MAU | 5万MAU |

このアプリの規模(会員数十人)なら Firebase 無料枠に収まり続けます。
